app = "crawlready-render-worker"
primary_region = "sea"

[build]
dockerfile = "apps/workers/render-worker/Dockerfile"

[env]
NODE_ENV = "production"
WORKER_CONCURRENCY = "5"

[processes]
worker = "node dist/index.js"

[[vm]]
memory = '2gb'
cpu_kind = 'shared'
cpus = 2
